import { useService } from '../src/serviceHooks';

describe('useService', () => {
  describe('return value', () => {
    it.todo('is [trigger, state, isActive]');
  });
});

describe('useChildService', () => {
  it.todo('creates a service for useService');

  describe('lifecycle', () => {
    it.todo('stops the service on unmount ');
    it.todo('stops any Observable handlings on unmount ');
  });
});
